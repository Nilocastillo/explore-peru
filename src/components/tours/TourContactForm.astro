---
import ConsultationForm from "@/components/forms/ConsultationForm.astro";

interface Props {
  title: string;
  duration: string;
  departure: string;
}

const { title, duration, departure } = Astro.props;
---

<ConsultationForm tour={title} duration={duration} departure={departure} />
